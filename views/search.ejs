<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <link rel="stylesheet" href="/css/search.css">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Search Results</title>
</head>
<body>
  <header class="header">
    <div class="logo">ByteCard</div>
    <nav class="nav">
      <a href="/" class="nav-link">Home</a>
      <a href="/login" class="nav-link">Login</a>
    </nav>
  </header>

  <div class="container">
    <h1 class="search_text">Cards Table</h1>
    <div class="results-container">
      <ul class="search-results">
        <% if (cards && cards.length > 0) { %>
          <% cards.forEach(function(card) { %>
            <li class="card">
              <div class="card-header">
                <span class="card-name"><%= card.cardname %></span>
                <span class="card-brand">ByteCard</span>
              </div>
              <div class="card-body">
                <div class="card-position"><%= card.card_position %></div>
              </div>
              <div class="card-footer">
                <span class="company-name"><%= card.card_company %></span>
                <span class="card-industry">Tag ID: <%= card.tagid %></span>
              </div>
            </li>
          <% }); %>
        <% } else { %>
          <li>No results found.</li>
        <% } %>
      </ul>
    </div>
  </div>
</body>
</html>